package working.structure;



import com.smarthousebuilder.forum.post.Post;

import java.util.ArrayList;
import java.util.List;

public class PostQuestion extends Post {

  /*
  public List<working.structure.Tag> tags = new ArrayList<>();
  public PostQuestion(int id, String content, String title, String author, String date) {
    super(id, content, title, author, date);
  }

  public boolean addTag(working.structure.Tag tag) {
    if (tags.contains(tag)) throw new forum.exceptions.InvalidTag();
    return false;
  }

   */

}